import { memo } from "react";
import isEqual from "react-fast-compare";

const AdvertiseCollection = () => {
  return <div>AdvertiseCollection</div>
}

export default memo(AdvertiseCollection, isEqual);
