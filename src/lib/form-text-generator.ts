export const REQUIRED_MSG = (label: string) =>
  "{{label}} is required" + { label };

export const PLACEHOLDER_MSG = (label: string) =>
  "Type in your {{label}}" + { label };

export const INVALID_MSG = (label: string) =>
  "Invalid {{label}}" + { label };

export const CREATED_SUCCESSFULLY_MSG = (label: string) =>
  "{{label}} is created successfully" + { label };

export const EDIT_SUCCESSFULLY_MSG = (label: string) =>
  "{{label}} is edited successfully" + { label };

export const DELETE_SUCCESSFULLY_MSG = (label: string) =>
  "{{label}} is deleted successfully" + { label };
